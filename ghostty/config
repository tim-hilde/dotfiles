font-family = "JetBrainsMono Nerd Font Mono"
font-style = Medium
font-size = 12

theme = catppuccin-mocha
alpha-blending = linear

cursor-style = block

link-url = true

title = ""
window-padding-x = 6
window-padding-y = 6

window-decoration = auto

clipboard-read = allow
clipboard-write = allow

shell-integration = detect
shell-integration-features = no-cursor, sudo, title

# macos
macos-titlebar-style = hidden
macos-auto-secure-input = true
macos-secure-input-indication = true

# keybindings

# change to previous dir
#keybind = option+cmd+left=text:dirhistory_back
#keybind = option+cmd+right=text:dirhistory_forward

# needed for snacks picker
keybind = option+shift+h=unbind
keybind = option+i=unbind

## history search
# keybind = "ctrl+r=text:h | fzf --preview="echo {}" --preview-window=down,3,wrap --height=60%\r"
# keybind = cmd+f=action:write_scrollback_file
